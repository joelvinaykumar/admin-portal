import React from 'react';
import { Typography } from 'antd';

const { Text } = Typography;

const Text = ({size,text, weight, lineHeight, letterSpacing}) => (
  <Text 
    title={text}
  />
)

export default Text;